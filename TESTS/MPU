#include <Arduino.h>

void setup()
{
  Serial.begin(115200);
  Serial.println("DAC TEST");
}

void loop()
{
  for (int i = 0; i < 256; i++)
  {
    dacWrite(25, i);
    delayMicroseconds(50);
  }

  for (int i = 255; i >= 0; i--)
  {
    dacWrite(25, i);
    delayMicroseconds(50);
  }
}
