File pngFile = SD.open(path);

if (!pngFile)
{
    Serial.println("PNG OPEN FAILED");
    return false;
}

bool result = display.drawPng(
    &pngFile,
    x,
    y
);

pngFile.close();

return result;
