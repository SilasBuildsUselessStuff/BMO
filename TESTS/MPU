#include <Arduino.h>

void setup()
{
  Serial.begin(115200);
  Serial.println("PWM TEST");
}

void loop()
{
  ledcAttach(26, 1000, 8); // Pin, Frequency, Resolution
  ledcWrite(26, 128);      // 50% Duty

  while(true)
  {
    delay(1000);
  }
}
